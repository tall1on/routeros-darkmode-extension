// Service worker placeholder for RouterOS Dark Mode extension.
//
// Currently, all functionality is handled by the content script. This file exists to keep the
// background entry point ready for future enhancements such as synchronized state storage,
// context menu commands, or scheduled refresh logic.
//
// Rollup will still bundle this stub to satisfy Chrome Manifest V3 requirements.

export const noop = () => {
    // Intentionally left blank.
};

noop();